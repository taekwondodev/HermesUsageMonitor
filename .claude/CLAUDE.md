## Dev cycle

### Issue tracker

GitHub Issues for `taekwondodev/HermesUsageMonitor`, operated through `gh`. See `docs/agents/issue-tracker.md`.

### Triage labels

The repository uses the five canonical dev-cycle labels without renaming: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, and `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context repository with root `CONTEXT.md` and system ADRs in `docs/adr/`. See `docs/agents/domain.md`.
